import HeroGiftingSeason from "./hero/HeroGiftingSeason";

const Hero = () => {
  // Always use generated images - no music
  return <HeroGiftingSeason useGeneratedImages={true} />;
};

export default Hero;
